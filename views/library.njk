{% extends 'layout.njk' %}

{% block pageTitle %}
    <title>{{ book.title }}</title>
{% endblock %}

{% block content %}
<section class='hero'>
        <a href='/add-new' class='navButton' role="button">+</a>
    <div class='hero__title'>
        <h1>Library.</h1>
    </div>
</section>
<main class='mainContent'>
    <ul class='mainContent__list'>
    {% for book in books %}
        <li class='mainContent__list mainContent__list--title'><a href='/info/{{ book.id }}'>{{ book.title }}</a></li>
        <p class='mainContent__list mainContent__list--author'>{{ book.author }}</p>
    {% endfor %}
    </ul>
    <section class='pagination'>
    {% if page > 1 %}
        <a href='?p={{ page - 1 }}' class='pagination-arrows'><<</a>
    {% endif %}
        <p>{{ page }}</p>
    {% if lastPage != true %}
        <a href='?p={{ page + 1 }}' class='pagination-arrows'>>></a>
    {% endif %}
    </section>
</main>
{% endblock %}
